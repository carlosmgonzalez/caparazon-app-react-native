import { ThemedText } from "@/presentation/themes/components/ThemedText";
import { View } from "react-native";

const AppScreen = () => {
  return (
    <View>
      <ThemedText>App Screen</ThemedText>
    </View>
  );
};

export default AppScreen;
